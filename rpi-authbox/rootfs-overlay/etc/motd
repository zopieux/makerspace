
  Welcome to [01;34mrpi-authbox OS[00m!

  /!\ This system is entirely in-memory and managed with systemd.
      All modifications will be lost upon reboot.

  # Overall health:
  $ systemctl status

  # System logs:
  $ journalctl -fel

  # Authbox software state:
  $ systemctl status -n40 authbox

  # System info (hosts, DNS, time & date):
  $ hostnamectl
  $ resolvectl
  $ timedatectl

  --
  zopi@

